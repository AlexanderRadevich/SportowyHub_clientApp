# Tasks: Fix ScrollView Tap Gesture

## Tasks

- [x] Create a `LinkButton` style in `Styles.xaml` (transparent background, no border, text-link appearance)
- [x] Replace `Label`+`TapGestureRecognizer` with `Button` using `LinkButton` style on `SearchPage.xaml`
- [x] Replace `Label`+`TapGestureRecognizer` with `Button` using `LinkButton` style on `HomePage.xaml`
- [x] Adjust Button padding and minimum size to match original Label appearance
- [x] Test tap reliability inside ScrollView on Android
- [x] Test tap reliability inside ScrollView on iOS
