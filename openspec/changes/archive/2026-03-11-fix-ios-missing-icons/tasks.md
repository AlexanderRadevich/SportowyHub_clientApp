## Tasks

- [x] Replace `stroke="currentColor"` with `stroke="#000000"` in all SVG files that use it (13 files)
- [x] Replace `.svg` with `.png` in all image references (XAML + C# converters) — root cause of iOS invisibility
- [x] Add explicit `Route` attributes to all ShellContent tabs (fixed `//profile` navigation error after login)
- [x] Build and test on iOS and Windows/Android
