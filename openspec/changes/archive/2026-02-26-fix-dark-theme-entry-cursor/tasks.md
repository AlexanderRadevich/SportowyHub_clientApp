## 1. Fix Entry cursor color

- [x] 1.1 Add `CursorColor` setter with `AppThemeBinding` (Light=`Primary`, Dark=`PrimaryDark`) to the implicit `Entry` style in `SportowyHub.App/Resources/Styles/Styles.xaml`

## 2. Verify

- [x] 2.1 Build the app (`dotnet build SportowyHub.App -f net10.0-android`)
