## 1. Update Filter Chip Close Button

- [x] 1.1 In `SportowyHub.App/Views/Search/SearchPage.xaml`, change the filter chip close `ImageButton` `HeightRequest` and `WidthRequest` from `14` to `10`
- [x] 1.2 In the same `DataTemplate`, change the `HorizontalStackLayout` `Spacing` from `6` to `4`

## 2. Verify

- [x] 2.1 Build the app with `dotnet build`
- [x] 2.2 Visually confirm the close button appears smaller and proportional on the search screen
