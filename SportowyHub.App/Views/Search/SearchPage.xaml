<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SportowyHub.ViewModels"
             xmlns:models="clr-namespace:SportowyHub.Models.Api"
             xmlns:localModels="clr-namespace:SportowyHub.Models"
             xmlns:controls="clr-namespace:SportowyHub.Controls"
             xmlns:res="clr-namespace:SportowyHub.Resources.Strings"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="SportowyHub.Views.Search.SearchPage"
             x:DataType="vm:SearchViewModel"
             Title="{x:Static res:AppResources.TabSearch}"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto,Auto,Auto,*" Padding="16">

        <Border Grid.Row="0"
                BackgroundColor="{AppThemeBinding Light={StaticResource SearchBarBackground}, Dark={StaticResource SearchBarBackgroundDark}}"
                StrokeThickness="{AppThemeBinding Light=0, Dark=1}"
                Stroke="{AppThemeBinding Light=Transparent, Dark={StaticResource BorderDark}}"
                Padding="8,4"
                Margin="0,8,0,16">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="14" />
            </Border.StrokeShape>
            <Grid ColumnDefinitions="Auto,*,Auto,Auto">
                <ImageButton Grid.Column="0"
                             Source="icon_back.svg"
                             HeightRequest="24"
                             WidthRequest="24"
                             Padding="8"
                             BackgroundColor="Transparent"
                             Command="{Binding GoBackCommand}"
                             VerticalOptions="Center">
                    <ImageButton.Behaviors>
                        <toolkit:IconTintColorBehavior TintColor="{AppThemeBinding Light={StaticResource Secondary}, Dark={StaticResource White}}" />
                    </ImageButton.Behaviors>
                </ImageButton>
                <Entry Grid.Column="1"
                       x:Name="SearchEntry"
                       AutomationId="SearchEntry"
                       Text="{Binding SearchText}"
                       Placeholder="{x:Static res:AppResources.SearchPlaceholder}"
                       BackgroundColor="Transparent"
                       FontSize="14"
                       VerticalOptions="Center"
                       Focused="OnSearchEntryFocused" />
                <ImageButton Grid.Column="2"
                             Source="icon_clear.svg"
                             HeightRequest="20"
                             WidthRequest="20"
                             Padding="8"
                             BackgroundColor="Transparent"
                             Command="{Binding ClearSearchCommand}"
                             IsVisible="{Binding SearchText.Length, Converter={StaticResource IntToBoolConverter}}"
                             VerticalOptions="Center">
                    <ImageButton.Behaviors>
                        <toolkit:IconTintColorBehavior TintColor="{AppThemeBinding Light={StaticResource Secondary}, Dark={StaticResource White}}" />
                    </ImageButton.Behaviors>
                </ImageButton>

                <Grid Grid.Column="3" VerticalOptions="Center" Margin="4,0,0,0">
                    <ImageButton Source="icon_filter.svg"
                                 HeightRequest="20"
                                 WidthRequest="20"
                                 Padding="8"
                                 BackgroundColor="Transparent"
                                 Command="{Binding OpenFilterCommand}"
                                 VerticalOptions="Center">
                        <ImageButton.Behaviors>
                            <toolkit:IconTintColorBehavior TintColor="{AppThemeBinding Light={StaticResource Secondary}, Dark={StaticResource White}}" />
                        </ImageButton.Behaviors>
                    </ImageButton>

                    <Border HeightRequest="16"
                            WidthRequest="16"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                            HorizontalOptions="End"
                            VerticalOptions="Start"
                            Margin="0,-2,-2,0"
                            IsVisible="{Binding HasActiveFilters}"
                            InputTransparent="True">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="8" />
                        </Border.StrokeShape>
                        <Label Text="{Binding ActiveFilterCount}"
                               FontSize="9"
                               TextColor="White"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"
                               HorizontalTextAlignment="Center" />
                    </Border>
                </Grid>
            </Grid>
        </Border>

        <ScrollView Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalScrollBarVisibility="Never"
                    IsVisible="{Binding HasActiveFilters}"
                    Margin="0,0,0,8">
            <HorizontalStackLayout Spacing="8"
                                   BindableLayout.ItemsSource="{Binding ActiveFilterChips}">
                <BindableLayout.ItemTemplate>
                    <DataTemplate x:DataType="localModels:ActiveFilterChip">
                        <Border Padding="8,4"
                                StrokeThickness="0"
                                BackgroundColor="{AppThemeBinding Light={StaticResource SearchBarBackground}, Dark={StaticResource SearchBarBackgroundDark}}">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="14" />
                            </Border.StrokeShape>
                            <HorizontalStackLayout Spacing="6">
                                <Label Text="{Binding Label}"
                                       FontSize="13"
                                       TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}"
                                       VerticalOptions="Center" />
                                <ImageButton Source="icon_clear.svg"
                                             HeightRequest="14"
                                             WidthRequest="14"
                                             BackgroundColor="Transparent"
                                             Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SearchViewModel}}, Path=RemoveFilterCommand}"
                                             CommandParameter="{Binding Key}"
                                             VerticalOptions="Center">
                                    <ImageButton.Behaviors>
                                        <toolkit:IconTintColorBehavior TintColor="{AppThemeBinding Light={StaticResource Secondary}, Dark={StaticResource White}}" />
                                    </ImageButton.Behaviors>
                                </ImageButton>
                            </HorizontalStackLayout>
                        </Border>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </HorizontalStackLayout>
        </ScrollView>

        <ActivityIndicator Grid.Row="2"
                           IsRunning="{Binding IsSearching}"
                           IsVisible="{Binding IsSearching}"
                           Color="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                           HorizontalOptions="Center"
                           Margin="0,0,0,8" />

        <ScrollView Grid.Row="3"
                    x:Name="SuggestionsView">
            <ScrollView.Triggers>
                <DataTrigger TargetType="ScrollView"
                             Binding="{Binding SearchText.Length, Converter={StaticResource IntToBoolConverter}}"
                             Value="True">
                    <Setter Property="IsVisible" Value="False" />
                </DataTrigger>
                <DataTrigger TargetType="ScrollView"
                             Binding="{Binding HasActiveFilters}"
                             Value="True">
                    <Setter Property="IsVisible" Value="False" />
                </DataTrigger>
                <DataTrigger TargetType="ScrollView"
                             Binding="{Binding HasSearchResults}"
                             Value="True">
                    <Setter Property="IsVisible" Value="False" />
                </DataTrigger>
            </ScrollView.Triggers>
            <VerticalStackLayout Spacing="24">

                <VerticalStackLayout Spacing="8"
                                     IsVisible="{Binding RecentSearches.Count, Converter={StaticResource IntToBoolConverter}}">
                    <Grid ColumnDefinitions="*,Auto">
                        <Label Text="{x:Static res:AppResources.SearchRecentSearches}" Style="{StaticResource SectionHeader}" />
                        <Label Grid.Column="1"
                               Text="{x:Static res:AppResources.SearchClearRecent}"
                               TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                               FontSize="13"
                               VerticalOptions="Center">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding ClearRecentSearchesCommand}" />
                            </Label.GestureRecognizers>
                        </Label>
                    </Grid>
                    <CollectionView ItemsSource="{Binding RecentSearches}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <Grid Padding="0,10">
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SearchViewModel}}, Path=SelectSuggestionCommand}"
                                            CommandParameter="{Binding}" />
                                    </Grid.GestureRecognizers>
                                    <Label Text="{Binding}"
                                           FontSize="14"
                                           TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="8">
                    <Label Text="{x:Static res:AppResources.SearchPopularSearches}" Style="{StaticResource SectionHeader}" />
                    <CollectionView ItemsSource="{Binding PopularSearches}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <Grid Padding="0,10">
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SearchViewModel}}, Path=SelectSuggestionCommand}"
                                            CommandParameter="{Binding}" />
                                    </Grid.GestureRecognizers>
                                    <Label Text="{Binding}"
                                           FontSize="14"
                                           TextColor="{AppThemeBinding Light={StaticResource TextPrimary}, Dark={StaticResource TextPrimaryDark}}" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>

            </VerticalStackLayout>
        </ScrollView>

        <ScrollView Grid.Row="3"
                    x:Name="ResultsScrollView"
                    Scrolled="OnResultsScrolled">
            <ScrollView.Triggers>
                <MultiTrigger TargetType="ScrollView">
                    <MultiTrigger.Conditions>
                        <BindingCondition Binding="{Binding SearchText.Length, Converter={StaticResource IntToBoolConverter}}" Value="False" />
                        <BindingCondition Binding="{Binding HasActiveFilters}" Value="False" />
                        <BindingCondition Binding="{Binding HasSearchResults}" Value="False" />
                    </MultiTrigger.Conditions>
                    <Setter Property="IsVisible" Value="False" />
                </MultiTrigger>
            </ScrollView.Triggers>
            <VerticalStackLayout>
                <FlexLayout x:Name="ResultsGrid"
                            Wrap="Wrap"
                            Direction="Row"
                            JustifyContent="SpaceBetween"
                            BindableLayout.ItemsSource="{Binding MappedSearchResults}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="localModels:SearchCardItem">
                            <controls:ListingCardView Margin="0,0,0,12"
                                                      Listing="{Binding Listing}"
                                                      HasCondition="{Binding HasCondition}"
                                                      ConditionText="{Binding ConditionText}"
                                                      ConditionBadgeColor="{Binding ConditionBadgeColor}"
                                                      TapCommand="{Binding Source={RelativeSource AncestorType={x:Type vm:SearchViewModel}}, Path=GoToListingDetailCommand}" />
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </FlexLayout>
                <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Padding="0,40"
                                     IsVisible="{Binding ShowNoResults}">
                    <Label Text="{x:Static res:AppResources.SearchNoResults}"
                           TextColor="{AppThemeBinding Light={StaticResource TextSecondary}, Dark={StaticResource TextSecondaryDark}}"
                           HorizontalTextAlignment="Center" />
                </VerticalStackLayout>
            </VerticalStackLayout>
        </ScrollView>

    </Grid>

</ContentPage>
